const inputElement = <input autoFocus />;

const buttonElement = <button onClick ={() => console.log('clicked')}>click me!</button>

const imageElement = <img src="./image.jpg" />;

const paragraph = <p className="text-red">text</p>;